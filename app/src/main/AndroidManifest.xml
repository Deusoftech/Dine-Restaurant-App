<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dinerestaurant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dine"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DineRestaurant">
        <activity android:name=".drinks"></activity>
        <activity android:name=".ordered" />
        <activity android:name=".food" />
        <activity android:name=".createNewPass" />
        <activity android:name=".home" />
        <activity android:name=".forgot_password" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity">


        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>